# Function: generateStreamId()

> **generateStreamId**(): `string`

Defined in: [abstractions/hooks.ts:98](https://github.com/actioncrew/streamix/blob/main/projects/libraries/streamix/src/lib/abstractions/hooks.ts#L98)

Generates a unique stream identifier.

IDs are stable within a single runtime session
and deterministic for tracing purposes.

## Returns

`string`
