/**
 * Compiler-owned component member used from rewritten Angular templates.
 *
 * Keep this identifier within Angular's template lexer grammar. Angular template
 * expressions do not accept the TypeScript-style `ɵ` prefix that we use for
 * runtime-only generated symbols.
 */
export const SX_SOURCE_REFERENCES_FIELD = '__sxRefs';
